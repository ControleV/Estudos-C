#include <stdio.h>

// Os tipos precisam ser declarados junto com a variável.
int main(){
    int _idade = 25;         //Variáveis de número inteiro
    int quantidade2 = 2;    
    float altura = 1.75;       // Variáveis de ponto flutuante menores
    double peso = 90.3;        // Variáveis de ponto flutuante maiores
    char letra = 'A';         // Armazena caractéres
    char nome[20] = "victor";      // Armazena strings via array de chars do tamanho 20

    _idade = 23;                 // Definindo um novo valor
}
